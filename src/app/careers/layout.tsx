import { buildPageMetadata } from "@/lib/seo";
export { default } from "@/app/components/common/SimpleLayout";

export const metadata = buildPageMetadata({
  title: "Careers",
  description: "Careers at GME Finance: join our team and grow with us.",
  path: "/careers",
});
